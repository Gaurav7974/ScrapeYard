# ScrapeYard#
ScrapeYard: an open‑source, config‑driven web‑crawling platform plus an AI assistant for generating crawlers.
Each crawler is a template + JSON config (static HTML, dynamic headless, API/JSON, sitemap) that users can run locally (Python, Colab) without touching core code.
